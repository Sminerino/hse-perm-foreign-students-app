import { StyleSheet } from 'react-native';

const ScreenHeaderButtonStyles = StyleSheet.create({
    icon: {
        width: 25,
        height: 25,
    },
    container: {
        padding: 15,
        alignItems: 'center',
        justifyContent: 'center',
    }
});
export default ScreenHeaderButtonStyles;